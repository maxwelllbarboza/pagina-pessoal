import { motion } from "framer-motion"

export default function SobreMim(){
    return(
        <section className="">
            <div>
                <MotionConfig.div>

                </MotionConfig.div>
            </div>
        </section>

    )
}