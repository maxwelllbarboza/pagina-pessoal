import { motion } from "framer-motion"

export default function SobreMim(){
    return(
        <section className="min-h-screen bg-g">
            <div>
                <MotionConfig.div>

                </MotionConfig.div>
            </div>
        </section>

    )
}